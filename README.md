# VNID-QR

Vietnam ID QR Code Generator
> ⚠️ **Disclaimer:** This app is an independent tool and is not affiliated with the Vietnamese Government or VNeID.


🌐 **Live Demo**: https://vanvixi.github.io/vnid-qr/

## Build local

```bash
# Web
fvm flutter build web --release

# Android
fvm flutter build apk --release

# iOS
fvm flutter build ios --release

# macOS
fvm flutter build macos --release

# Windows
fvm flutter build windows --release

# Linux
fvm flutter build linux --release
```

## Run local

```bash
fvm flutter run -d chrome
```

